使用PSRAM：菜单配置开启PSRAM，并且mode选择octal
使用spiff: 1.创建分区表后
           2.在菜单中开启flash的16M（默认2M）
           3.以及添加创建的分区表
           4.修改 CMakeLists.txt 实现自动打包烧录
使用lvgl显示gif图片:菜单开启LV_USE_FS_POSIX和LV_USE_GIF
让lvgl在PSRAM中解码:需要在菜单中开启lvgl使用PSRAM
统计任务运行时间以及占用情况：菜单中开启FreeRTOS的任务统计宏
性能优化：Optimization Level中开启Optimize for performance (-O2)，将160mhz修改为240mhz


问题：
注意一个点：esp32的cpu读取数据/定义变量是以小端模式读取的，即本质上数据在内存中以大端模式分布的，但是esp32的cpu读取出来并且放到另一块内存中是小端模式，即使会调换字节序。
但是如果直接将以大端模式解码后的数据指针直接赋值给SPI绘图函数，SPI传输的就是正确的大端传输
对于一个**32 位的大数组**（例如 `uint32_t array[1000]`），大端和小端的区别在于：**数组中每一个单独的 32 位元素内部，其 4 个字节的排列顺序是相反的。**
SPI发送是先发低地址
所以原本的st7735_test_pattern函数中，直接定义了颜色变量，但是cpu定义数据是以小端模式，然后直接进行SPI发送了，但是ST7735屏幕接收可能需要大端模式，但是直接传递以大端模式下jpeg解码后的图像数据的指针给绘图函数，就符合屏幕接收的需求